-- Remote migration history placeholder.
--
-- This version is already recorded in the production Supabase migration
-- history, but the matching file was missing from main. Keep this no-op so
-- `supabase db push` can reconcile local files with the remote history table.

SELECT 1;
