-- keep-migration-version: already recorded on production schema_migrations
-- Remote migration history placeholder (applied by a parallel worktree).
-- Keep this no-op file so `supabase db push` can reconcile local with remote.

SELECT 1;
