-- Remote migration history placeholder.
--
-- This version is already recorded in the production Supabase migration
-- history, but the corresponding SQL file was not present in the repository.
-- Keep this no-op file so `supabase db push` can reconcile local migrations
-- with the remote history table without altering schema on fresh databases.

SELECT 1;
